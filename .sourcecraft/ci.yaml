version: "1.0"

tasks:
  - name: build
    steps:
      - name: build-site
        cube: alpine:3.18
        script: |
          # Nothing to build for static site
          echo "Static site ready for deployment"
          
  - name: deploy
    steps:
      - name: deploy-site
        cube: sourcecraft/sites:latest
        script: |
          echo "Deploying site to SourceCraft Sites"
